project_name: servcache
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: servcache
    main: .
    binary: servcache
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: servcache
    builds: [servcache]
    name_template: "servcache_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/servcache:{{.Tag}}"
      - "ghcr.io/vyuvaraj/servcache:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=ServCache"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/ServCache"

brews:
  - name: servcache
    repository:
      owner: vyuvaraj
      name: homebrew-serv
    homepage: "https://github.com/vyuvaraj/ServCache"
    description: "Distributed caching service from the ServVerse platform"
    license: "Apache-2.0"
    install: |
      bin.install "servcache"
    test: |
      system "#{bin}/servcache", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-serv
    homepage: "https://github.com/vyuvaraj/ServCache"
    description: "Distributed caching service from the ServVerse platform"
    license: "Apache-2.0"


nfpms:
  - id: servcache-pkg
    package_name: servcache
    builds: [servcache]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/ServCache
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: High-performance distributed caching layer from the ServVerse platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/servcache/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
