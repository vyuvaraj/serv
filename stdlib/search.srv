// Serv Standard Library: Multi-backend Search Client
// Import: import { query, index } from "stdlib/search.srv"

export fn query(q: string, options: map) -> any {
    return search.query(q, options)
}

export fn index(id: string, data: any) -> any {
    return search.index(id, data)
}
