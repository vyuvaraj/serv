project_name: servtrace
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: servtrace
    main: .
    binary: servtrace
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: servtrace
    builds: [servtrace]
    name_template: "servtrace_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/servtrace:{{.Tag}}"
      - "ghcr.io/vyuvaraj/servtrace:latest"
    dockerfile: Dockerfile
    build_flag_templates:
      - "--label=org.opencontainers.image.title=ServTrace"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/ServTrace"

brews:
  - name: servtrace
    repository:
      owner: vyuvaraj
      name: homebrew-serv
    homepage: "https://github.com/vyuvaraj/ServTrace"
    description: "Distributed tracing and observability service from the ServVerse platform"
    license: "Apache-2.0"
    install: |
      bin.install "servtrace"
    test: |
      system "#{bin}/servtrace", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-serv
    homepage: "https://github.com/vyuvaraj/ServTrace"
    description: "Distributed tracing and observability service from the ServVerse platform"
    license: "Apache-2.0"
