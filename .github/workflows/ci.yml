name: ServLock CI

on:
  push:
    branches: ["main", "feature/*"]
  pull_request:
    branches: ["main"]

jobs:
  ci:
    name: Build · Test · Vet
    runs-on: ubuntu-latest

    steps:
      - name: Checkout ServShared
        uses: actions/checkout@v4
        with:
          repository: vyuvaraj/ServShared
          path: ServShared

      - name: Checkout ServLock
        uses: actions/checkout@v4
        with:
          path: ServLock

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true
          cache-dependency-path: ServLock/go.sum

      - name: Build
        working-directory: ServLock
        run: go build ./...

      - name: Vet
        working-directory: ServLock
        run: go vet ./...

      - name: Test
        working-directory: ServLock
        run: go test ./... -timeout 120s
